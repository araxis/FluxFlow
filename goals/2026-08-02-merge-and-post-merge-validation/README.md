# Goal: Merge Pull Request 65 And Prove The Merged Release Candidate

## Status

- State: completed
- Date: 2026-08-02
- Repository: `C:\Projects\FluxFlow`
- Pull request: 65
- Base branch: `main`
- Candidate branch: `work/major-surface-reset`
- Reviewed candidate head:
  `650dc1b916f3fee3d2f03d2a043be0312b45a7b8`
- Scope: approval attempt, normal merge, exact-merge verification, clean
  post-merge release rehearsal, real-provider validation, package rehearsal,
  and durable evidence
- Explicitly excluded: tag creation, release creation, package publication,
  feed publication verification, unrelated cleanup, and new product work

## Objective

Complete the already reviewed release-candidate transition without weakening
any gate. Reconfirm that pull request 65 still points at the exact reviewed
head, attempt the user-authorized approval through the normal hosting-service
identity, merge it with the repository's established merge-commit strategy,
and prove the exact resulting `main` commit from a clean detached worktree.

The post-merge proof must cover the ordinary solution, release governance,
formatting, dependency vulnerability reporting, both project-owned real
networked-relational provider suites, and a complete package rehearsal for all
59 manifest packages. Record the exact immutable evidence in the goal and
memory. Do not create or push a tag, create a release, publish a package, use an
administrative merge bypass, impersonate another reviewer, or introduce a
production change.

## Context And Accepted Baseline

The preceding final-review goal established all of the following at candidate
head `650dc1b916f3fee3d2f03d2a043be0312b45a7b8`:

- the pull request is open, ready rather than draft, mergeable, and clean;
- its ordinary remote `build-test` check passed on the exact head in run
  `30753529432`;
- no review or review thread exists;
- the branch and remote head are synchronized;
- no unresolved P0, P1, or bounded P2 review finding remains;
- the complete Release solution passed 2,495 tests across 66 projects;
- Release governance passed 127 tests;
- the public API baseline and 59-package inventory are consistent;
- the controlled CI-style build covered 134 targets with no warning or error;
- the previous clean-checkout real-provider suites passed 89 durable-input and
  117 durable-output tests with zero skip; and
- pull request 65 contains the corrections for lifecycle state restoration and
  exact-one durable-input store ownership.

The current repository settings allow merge commits, squash merges, and rebase
merges. Recent `main` history consistently records pull requests through merge
commits. `main` currently has no configured branch-protection object. This goal
must preserve the stronger established review and validation behavior anyway;
the absence of a server-side rule is not authority to skip a gate.

## Non-Negotiable Principles

1. Keep the operation explicit, observable, and reversible until the remote
   merge itself is performed.
2. Use the existing repository scripts and test projects. Add no package,
   abstraction, reflection, hidden discovery, workflow engine, or orchestration
   framework.
3. Do not change runtime code, schemas, public APIs, versions, package
   identities, provider behavior, or release scripts during this goal.
4. Never force-push, rewrite history, delete the candidate branch, bypass a
   policy, or use an administrative merge override.
5. Never obtain or repurpose another identity or credential to manufacture an
   approval. If the hosting service rejects self-approval, record the exact
   limitation and continue only if approval is not required, the user has
   explicitly authorized the merge, and every objective gate is green.
6. Do not expose connection strings, generated credentials, full secret values,
   or package-feed credentials in commands, logs, goal text, or memory.
7. Provider runners must own their isolated server resources and clean them in
   `finally`. Never use their diagnostic keep-container option.
8. Keep ordinary CI server-free. Run the two external-provider suites only in
   this explicit post-merge release proof.
9. The authoritative proof target is the immutable merge commit returned by
   the remote service, not a mutable local branch name.
10. Stop immediately on a changed candidate head, failing required check,
    unclean merge state, unresolved review thread, unexpected base movement,
    merge-parent mismatch, test failure, skipped provider test, vulnerability,
    package rehearsal failure, leaked resource, or dirty detached worktree.
11. Do not tag, release, publish, or verify a public feed in this goal.

## Expected Repository Mutations

Allowed mutations are narrowly bounded:

- the remote merge commit that closes pull request 65;
- a saved goal and final memory evidence;
- a neutral evidence branch and follow-up pull request if committed evidence is
  required after the merge; and
- ignored local build, package, and test output owned by the validation run.

The final evidence should follow the repository's normal collaboration model:

1. keep this goal file locally while the release candidate is merged;
2. after successful post-merge proof, create a neutral evidence branch from the
   exact current `origin/main`;
3. update this goal to complete and add the final memory record;
4. commit only goal/memory evidence with a neutral subject;
5. push the evidence branch and open a ready pull request;
6. require ordinary remote CI on that exact evidence head;
7. merge the evidence pull request normally; and
8. fast-forward local `main` to the final remote result.

This second merge changes documentation evidence only. Do not recursively rerun
the full external-provider and 59-package proof after the evidence merge. The
ordinary remote check on the evidence-only head is the appropriate final gate;
the memory record must state the exact code-bearing merge commit on which the
full release proof ran.

## Phase 1: Immutable Pre-Merge Gate

Fetch the remote and capture:

- current local branch and clean/dirty status;
- `origin/main` object id;
- pull-request state, draft state, base/head branches, and head object id;
- mergeability and merge-state status;
- review decision and submitted reviews;
- every review thread and its resolution state;
- every current status check and its exact head association; and
- repository merge-mode settings.

Require all of the following before approval or merge:

- pull request 65 is open and not draft;
- base is `main` and head is `work/major-surface-reset`;
- head is exactly
  `650dc1b916f3fee3d2f03d2a043be0312b45a7b8`;
- local candidate and remote candidate resolve to that same object;
- mergeability is `MERGEABLE` and merge state is `CLEAN`;
- the required `build-test` check is completed successfully on that head;
- no review has requested changes;
- every review thread is resolved, with pagination explicitly exhausted;
- no new commit or base divergence appeared after the final review; and
- no tag, release, or publication action is already in progress for this
  candidate.

If any condition is false, do not approve or merge. Diagnose and report the
specific changed fact. Do not silently refresh the expected head to accept an
unreviewed commit.

## Phase 2: Authorized Approval Attempt

Submit a normal `APPROVE` review for pull request 65 using the already
authenticated repository identity. Do not use an alternate account, token,
browser profile, service identity, or administrative endpoint.

Handle the result explicitly:

- If approval succeeds, fetch the PR again and verify the approval is attached
  to the exact candidate head.
- If the hosting service rejects it because the authenticated identity authored
  the pull request, record the exact refusal as an expected platform ownership
  constraint. Do not misrepresent the result as an approval.
- Any other approval failure is a stop condition until its cause is understood.

A self-approval refusal does not by itself block this user-authorized merge
because the repository has no required-approval rule. Proceed only while every
objective pre-merge gate remains green and without an administrative bypass.

## Phase 3: Normal Merge

Immediately before merging, fetch and repeat the immutable pre-merge checks.
Capture the current `origin/main` id as `PRE_MERGE_MAIN` and the reviewed head as
`REVIEWED_HEAD`.

Merge pull request 65 with the normal merge-commit mode because that is the
established first-parent history. Do not squash, rebase, delete the source
branch, queue a different head, or pass an administrative override.

After the hosting service reports success:

1. fetch all updated refs;
2. require the pull request state to be `MERGED`;
3. capture its immutable `mergeCommit` object id as `CODE_MERGE_COMMIT`;
4. require `origin/main` to equal `CODE_MERGE_COMMIT`;
5. require the merge commit to have exactly two parents;
6. require parent one to equal `PRE_MERGE_MAIN`;
7. require parent two to equal `REVIEWED_HEAD`;
8. require `REVIEWED_HEAD` to be an ancestor of `CODE_MERGE_COMMIT`;
9. verify the merge commit's tree matches the reviewed head when the base did
   not change; and
10. verify no tag or release was created as a side effect.

Switch the primary checkout to local `main` and update it only by fast-forward
to `origin/main`. Do not reset, discard, or overwrite unrelated local work.

## Phase 4: Clean Detached-Worktree Proof

Create a unique absolute directory under the system temporary root and add a
detached worktree at `CODE_MERGE_COMMIT`. Resolve and verify the target path
before removal. The directory must not be the repository root, a home root, or
another broad location.

Within that detached worktree:

1. require `HEAD` to equal `CODE_MERGE_COMMIT`;
2. require tracked status to be clean before validation;
3. record the selected SDK from `global.json` resolution;
4. restore `FluxFlow.sln`;
5. build `FluxFlow.sln` in Release with
   `ContinuousIntegrationBuild=true`, no restore, and serialized project
   execution to avoid shared-output contention;
6. test the complete solution in Release without rebuilding;
7. test the complete Release-governance project explicitly without rebuilding;
8. run solution-wide formatting verification without rewriting files;
9. run direct and transitive vulnerability reporting for the solution;
10. run the durable-input networked-relational integration runner with explicit
    license acceptance;
11. run the durable-output networked-relational integration runner separately
    with explicit license acceptance;
12. require both runners to execute at least one test, report zero failure and
    zero skip, and remove their owned server containers; and
13. require the worktree's tracked status to remain clean.

Use the repository's established test invocation because the pinned SDK and
test projects retain the existing runner configuration.

Canonical commands from the detached root are:

```powershell
dotnet restore FluxFlow.sln
dotnet build FluxFlow.sln --configuration Release --no-restore `
  -p:ContinuousIntegrationBuild=true -m:1
dotnet test FluxFlow.sln --configuration Release --no-build --no-restore
dotnet test tests/FluxFlow.Release.Tests/FluxFlow.Release.Tests.csproj `
  --configuration Release --no-build --no-restore
dotnet format FluxFlow.sln --verify-no-changes --no-restore
dotnet list FluxFlow.sln package --vulnerable --include-transitive
powershell -NoProfile -ExecutionPolicy Bypass `
  -File ./tests/FluxFlow.Engine.DurableInput.TSql.IntegrationTests/run-integration.ps1 `
  -AcceptLicense
powershell -NoProfile -ExecutionPolicy Bypass `
  -File ./tests/FluxFlow.Engine.DurableOutput.TSql.IntegrationTests/run-integration.ps1 `
  -AcceptLicense
```

Do not run the two provider suites concurrently. Their runners are the sole
owners of image selection, ephemeral credentials, readiness, random loopback
ports, isolated databases, no-skip enforcement, digest output, and cleanup.

## Phase 5: Complete 59-Package Publication Rehearsal

Still in the detached worktree, create a fresh package source under the system
temporary root. Do not reuse repository `artifacts`, an earlier rehearsal
source, or a public feed as the current-package source.

Read `eng/packages.json` in manifest order and require exactly 59 unique
aliases, package ids, project paths, and tag prefixes. For each entry:

1. require the project path to exist and remain under the detached repository;
2. resolve its declared version through the existing release helper;
3. run release preflight to prove manifest, version, tag name, and changelog
   release-note coverage;
4. run tag preparation only to prove resolution without creating a tag;
5. seed the fresh source by packing the already built project in Release with
   `--no-build`;
6. require one package archive and one symbol archive for the resolved id and
   version; and
7. never push a tag or call the release workflow.

After all packages are seeded, run the existing fast dry-run for every manifest
alias in the same dependency order:

- use `-SkipSolutionBuild` because Phase 4 already produced the controlled
  build and tests;
- use the fresh source as `-PackageSource`;
- include the fresh source plus the configured public dependency source as
  restore sources;
- require archive inspection, symbol validation, package-only consumer restore
  and build, and local feed verification to succeed for each package; and
- require `DRY_RUN_OK` for every expected package id.

The rehearsal must finish with:

- 59 successful preflights;
- 59 successful prepare-only tag resolutions;
- 59 package archives and 59 symbol archives in the fresh source;
- 59 successful dry-runs;
- no duplicate package id/version ambiguity;
- no tag, release, package publication, or public-feed mutation; and
- no tracked worktree change.

Binary-compatibility tooling is not used against an invented same-version
published baseline. The accepted source-declaration baseline and release
governance remain authoritative for this intentionally breaking major reset.
Do not weaken package validation to silence an unexpected compatibility error.

## Phase 6: Cleanup And Evidence Integrity

After every validation command has completed:

1. inspect running/stopped containers for the exact runner-owned name prefixes;
2. require that neither integration runner left an owned container;
3. inspect detached-worktree status and require no tracked change;
4. remove the detached worktree through the repository worktree command;
5. prune stale worktree metadata;
6. verify the exact detached path no longer exists;
7. remove only the exact fresh package-source directory after results have been
   summarized;
8. verify the primary worktree still contains only the intentionally uncommitted
   goal/evidence files; and
9. never recursively remove a computed path until its absolute value has been
   checked to remain under the intended temporary root.

If validation fails, preserve only the minimum diagnostic output necessary to
understand it. Do not retain a provider container unless the user separately
authorizes diagnostic retention.

## Phase 7: Durable Goal And Memory Record

Update this file to `State: complete` only after the code-bearing merge and
every Phase 4-6 gate pass. Append exact completion evidence including:

- pre-merge `origin/main`, reviewed head, check run, and review-thread state;
- approval result, including a truthful self-approval refusal if applicable;
- code-bearing merge commit and both parent ids;
- merge method and proof that no override was used;
- SDK, restore/build counts, test counts, warnings, and durations;
- governance count and result;
- formatting and vulnerability results;
- durable-input and durable-output provider counts, skip counts, tested image
  digest, and cleanup result;
- package manifest/preflight/prepare/seed/dry-run counts;
- archive and symbol archive counts;
- detached path and proof of its removal;
- explicit confirmation that no tag, release, publication, feed mutation, or
  retained container occurred; and
- any bounded non-product correction required to make the evidence truthful.

Create `memory/291-pr-65-merge-and-post-merge-validation.md` with the durable
summary. Update only concise cross-references/facts in:

- `memory/00-index.md`;
- `memory/01-current-state.md`;
- `memory/04-architecture-decisions.md` only if a genuinely new durable
  decision was made; and
- `memory/07-progress-log.md`.

Do not update the documentation site merely to repeat execution evidence. If a
release-contract defect is discovered, stop and fix it in a separately reviewed
change instead of silently rewriting documentation during a merge operation.

## Phase 8: Evidence-Only Collaboration Round

From synchronized `main`, create neutral branch:

```text
chore/record-merge-validation
```

Stage only the completed goal and memory files. Before committing:

- run `git diff --check` over the staged patch;
- scan new project-visible names and text for prohibited external branding;
- confirm no runtime, project, package, workflow, schema, version, or test file
  is staged; and
- rerun the Release-governance project because it owns goal/memory and release
  documentation contracts.

Commit with neutral subject:

```text
Record merge validation evidence
```

Push normally, open a ready pull request to `main`, and require its ordinary
remote CI to pass on the exact evidence head. Confirm clean merge state and no
unresolved review thread, then merge it with the normal merge-commit strategy.
Do not use an administrative override. Fast-forward local `main` and require a
clean synchronized worktree.

The evidence-only pull request does not require the external-provider or
manifest-wide package rehearsal to repeat. Its ordinary solution CI and
Release-governance gate prove that the documentation-only recording commit did
not damage the repository.

## Failure And Stop Conditions

Stop without merging pull request 65 if:

- its head differs from the reviewed object id;
- its base, state, or draft status changed unexpectedly;
- mergeability is not clean;
- a required check is absent, pending, stale, cancelled, or unsuccessful;
- a change-request review or unresolved review thread exists;
- the remote base changed in a way that invalidates the reviewed merge result;
  or
- approval fails for any reason other than the authenticated author being
  prohibited from approving their own pull request.

Stop after the merge and do not claim release readiness if:

- merge parents do not match the captured base and reviewed head;
- local `main` cannot fast-forward to the exact remote merge commit;
- clean-checkout restore, build, tests, governance, format, or vulnerability
  checks fail;
- either real-provider runner fails, skips tests, leaks an owned resource, or
  cannot execute;
- any package preflight, archive, symbol, consumer, or feed verification fails;
- the detached worktree becomes dirty; or
- a tag, release, or publication is created unexpectedly.

Do not “fix forward” production code directly on `main`. Record the blocker and
use a normal focused branch and reviewed pull request.

## Acceptance Criteria

This goal is complete only when:

- the detailed goal existed before the remote merge mutation;
- pull request 65 was reconfirmed at the exact reviewed head;
- authorized approval was submitted or the platform's self-approval rejection
  was truthfully recorded;
- the pull request was merged normally with no administrative bypass;
- the code merge commit has the exact expected base/head parents;
- local and remote `main` were synchronized by fast-forward;
- the clean detached proof ran at the exact code merge commit;
- restore and serialized CI-style Release build passed with zero warning/error;
- all solution and Release-governance tests passed;
- solution formatting and direct/transitive vulnerability checks passed;
- both real-provider suites passed with zero skips and cleaned their resources;
- all 59 manifest packages passed preflight, prepare-only resolution, pack,
  archive/symbol inspection, consumer smoke, and local feed verification;
- the temporary worktree and package source were safely removed;
- the completed goal and memory evidence were committed through a normal
  evidence-only pull request whose exact head passed remote CI;
- the evidence-only pull request was normally merged and local `main` is clean
  and synchronized;
- no tag, release, package publication, public-feed mutation, or retained
  provider resource occurred; and
- the repository is ready for a separate explicit release/tag decision.

## Deliberately Deferred

- Selecting which packages and versions to publish.
- Creating or pushing any package tag.
- Dispatching the release workflow.
- Creating release records or uploading assets.
- Publishing packages or verifying public indexing.
- New features, refactoring, performance work, compatibility shims, or provider
  additions.

## Completion Evidence

### Merge integrity

- Pull request 65 was reconfirmed open, ready, clean, and mergeable at reviewed
  head `650dc1b916f3fee3d2f03d2a043be0312b45a7b8`, based on
  `6ffc668b8054848f6c8e637005b10bcb4ee96689`.
- Ordinary CI run `30753529432` succeeded on that exact head. There were no
  reviews, review comments, or unresolved review requests.
- The authorized approval attempt was rejected by the hosting service with
  `Review Can not approve your own pull request`. No alternate identity or
  administrative bypass was used.
- Pull request 65 merged normally at `2026-08-02T15:32:09Z` as
  `7e64962256dbef873120e3dd34635dc13c24b4fb`.
- The merge parents are exactly the captured base and reviewed head. Its tree
  `163cb252abe4eae66a7338d3e6370b26a83032e5` equals the reviewed-head tree.
- Local `main` fast-forwarded to the remote merge result.

### Findings discovered by the post-merge proof

The proof stopped and used normal focused correction pull requests twice. No
production change was written directly to `main`.

1. The first clean full-suite run found a load-sensitive source-completion test
   ordering assumption. The production cancellation primitive already had
   direct coverage; the test released capacity immediately after requesting
   cancellation and did not causally await the blocked third emission's exit.
   A focused run passed, while 12 concurrent executions reproduced one timeout.
   Pull request 66 added explicit third-emission exit synchronization without a
   sleep, retry, timeout increase, public API change, or production change.
   Its head `0240d84f07548eaf3d223da4add586501adc4ed6` passed remote CI run
   `30755487188` and merged normally as
   `f3907fc0478957c31be8baa5662f85c360ea4580`.
2. The first manifest-wide package rehearsal passed the first 53 consumers and
   then found `FluxFlow.Engine.DurableInput` loading a stale same-version
   dependency graph from the machine-wide package cache. The newly packed
   `FluxFlow.Engine.7.0.0.nupkg` correctly required `FluxFlow.Nodes` 4.0.0;
   the stale cached graph required 3.0.1. Pull request 67 made the consumer
   smoke helper restore through a work-directory-local cache with cache reuse
   disabled, matching the existing feed verifier's isolation. Focused tests
   passed 2/2, the exact failing consumer produced `SMOKE_OK`, governance
   passed 127/127, and remote CI run `30758310177` succeeded on exact head
   `b8b26ae7edda7167601c6d660c76b7536edfeee1`. It merged normally as
   `ceedc36f6be53c11c89ddc49fe8f8b8fd427d227`.

### Runtime and provider proof

The runtime-bearing post-merge commit
`f3907fc0478957c31be8baa5662f85c360ea4580` passed from a clean detached
worktree:

- restore: 134 projects, zero warnings and errors;
- serialized Release CI build: 134 projects, zero warnings and errors;
- complete Release solution: 2,495/2,495 tests across 66 projects, zero
  warnings;
- Release governance: 127/127 tests, zero warnings;
- solution formatting: no changes required;
- direct/transitive vulnerability scan: no vulnerable package under the
  configured sources;
- real durable-input provider: 89/89 passed, zero failed/skipped;
- real durable-output provider: 117/117 passed, zero failed/skipped; and
- both providers used tested server image digest
  `sha256:ba4c8329f48fb8f02e1416be6a930ebfd71268caee78aa985f3af4315e457c89`
  and removed their owned containers.

Pull request 67 changed only release validation and its test, so it did not
invalidate the real-provider runtime result.

### Final exact-commit proof

Final merged commit `ceedc36f6be53c11c89ddc49fe8f8b8fd427d227` then
passed from a new clean detached worktree:

- restore and serialized Release CI build: 134 projects, zero warnings/errors;
- decisive complete Release solution run: 2,495/2,495 across 66 projects,
  zero warnings;
- Release governance: 127/127, zero warnings;
- full-solution formatting: no changes required;
- direct/transitive vulnerability scan: no vulnerable package;
- package inventory: exactly 59 unique aliases, ids, project paths, and tag
  prefixes;
- release preflight: 59/59;
- tag resolution with `-PrepareOnly`: 59/59, with no tag creation;
- fresh package seeds: 59/59 package archives and 59/59 symbol archives;
- isolated-cache package dry-runs: 59/59, including archive inspection, symbol
  validation, package-only consumer restore/build/load, and local feed
  verification; and
- final detached Git status: clean.

One first full-suite attempt on the final commit timed out in
`Flow_without_observers_still_completes_when_a_node_errors` during concurrent
host load. The exact test then passed 20/20 consecutive focused executions,
remote CI was already green on the same commit, and the decisive subsequent
full run passed 2,495/2,495. No code or timeout was changed for that
non-reproducing event.

### Cleanup and release boundary

- Both detached worktrees were removed through repository worktree cleanup and
  verified absent.
- Both temporary package sources, all package/symbol archives, consumer caches,
  diagnostics, logs, and temporary orchestration scripts were removed.
- No provider container created by this goal remains.
- No tag points at the final commit, and the latest existing release predates
  this execution.
- No tag, release, package publication, public-feed write, or release workflow
  dispatch occurred.
- This completed goal, documentation clarification, and memory record are the
  evidence-only change. Their exact evidence head, remote CI result, and normal
  merge are intentionally represented by repository and pull-request metadata
  rather than recursively embedding a commit id into its own content.
